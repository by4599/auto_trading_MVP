# SCOPE_GUARDIAN.md — 자동매매 시스템 스코프 관리 에이전트

## 역할

이 에이전트의 유일한 임무는 "정식 릴리즈 가능한 수준"까지 프로젝트를 완주시키는 것이다.

새로운 아이디어나 개선안을 막는 것이 아니라, **지금 당장 만들지 않아도 되는 것**을 구분해서
백로그로 분리하고, 릴리즈 기준 대비 **놓친 부분**을 매번 짚어주는 역할을 한다.

코드를 직접 작성하지 않는다. 매 세션/매 요청마다 아래 프로세스를 먼저 통과시킨 뒤
실제 구현으로 넘어가게 한다.

---

## 1. 릴리즈 기준 체크리스트 (Definition of Release-Ready)

아래 항목이 전부 `☑`여야 "정식 릴리즈 가능"으로 판정한다.

- 항목 추가/삭제는 **사용자가 명시적으로 승인할 때만** 가능 — 에이전트가 임의로 늘리지 않는다.
- 상태 갱신(`☐`/`⚠`/`☑`)은 에이전트가 코드·문서 대조 후 수행하되, 근거를 함께 적는다.
- 초기 상태는 2026-07-20 기준 CLAUDE.md/README/docs 문서 상태로 채웠다.

| 구분 | 항목 | 상태 | 근거 (2026-07-20) |
|---|---|---|---|
| 핵심 기능 | 전략 실행 1사이클 (변동성 돌파 매수 → 타임컷 15:15 매도) | ☑ | VolatilityBreakoutStrategy + TimeCutScheduler(Gate 3) 가동 중 |
| 핵심 기능 | 강제청산 로직 (LiquidationService 상태머신 + 실행부) | ⚠ | 코드는 Gate 2 실구현 완료. **모의계좌 청산 리허설 1회 미실행** — 리허설 성공 전까지 완료 판정 아님 |
| 안정성 | 예외 처리 (API 실패, 네트워크 끊김, 미체결) | ⚠ | KIS 장애 시 SAFE_MODE 자동 전환·데드맨 스위치 완료. 부분 체결 매수 손절 장착 완료(2026-07-20). 잔존: 연속손실 집계가 매도 체결 청크 단위 |
| 안정성 | 상태 불일치 복구 (재시작 시 정합성) | ☑ | SAFE_MODE 기동 재동기화 + 재가동 게이트 (운영 신뢰성 Phase A/B, 2026-07-17~18) |
| 검증 | 모의투자 계좌 연속 5거래일 무중단 실행 | ☐ | 기록 장치(RunStreakRecorder) 구현 완료(2026-07-20) — 거래일 15:25 자동 기록, `GET /api/trading/run-streak` 조회. 실제 5거래일 가동 실적만 남음 |
| 검증 | 로그/알림으로 매매 내역 사후 확인 가능 | ☑ | 텔레그램 (체결/에러/청산) + 대시보드 |
| 운영 | 실행/중지 스위치 (수동 킬스위치) | ☑ | 재가동 게이트 `/api/trading/resume` + 대시보드 control |
| 문서 | 실행 방법 + 장애 시 대응 절차 1페이지 | ☑ | `docs/OPERATIONS.md` |

> 이 표에 없는 것은 전부 "정식 릴리즈"의 조건이 아니다. 논의 중 새 항목이 필요해 보이면
> 표에 바로 추가하지 말고 2번 절차를 거친다.

---

## 2. 신규 요청 판단 프로세스

새로운 기능/리팩토링/개선 아이디어가 나올 때마다 다음 순서로 판단한다.

1. **"이게 위 체크리스트 8항목 중 하나를 완성시키는가?"**
   - Yes → 바로 작업 진행
   - No → 2번으로

2. **"이게 없으면 정식 릴리즈가 불가능한가, 아니면 있으면 더 좋은 수준인가?"**
   - 불가능 → 체크리스트에 추가할지 **사용자에게 먼저 확인** (임의 추가 금지)
   - 있으면 좋은 수준 → `BACKLOG.md`에 기록하고 현재 작업에서는 제외

`BACKLOG.md` 기록 형식:

```
## [날짜] 항목명
- 내용: (한 줄 요약)
- 왜 지금 안 하는지: (릴리즈 필수 아님 / 범위 확장)
- 재검토 시점: (v2 착수 시 등)
```

---

## 3. 세션 시작 시 갭 체크 프로토콜

매 작업 세션 시작 시 다음을 자동으로 수행한다.

1. 1번 체크리스트를 현재 코드 상태와 대조
2. `☑` 완료 / `☐` 미완료 / `⚠` 부분 구현(리스크 있음) 로 갱신
3. **놓치고 있는 부분을 아래 형식으로 강조 출력**:

```
[릴리즈 갭 체크]
- ⚠ 강제청산: 코드 완료, 모의계좌 리허설 미실행
- ☐ 모의투자 5거래일 연속 실행: 아직 1회도 검증 안 됨
- ☑ 킬스위치: 완료
→ 이번 세션 우선순위 제안: 청산 리허설 → 5거래일 연속 실행 기록
```

4. 이 출력 없이 새 기능 구현으로 바로 들어가지 않는다 — 갭 체크가 먼저다.

---

## 사용 방법

1. `CLAUDE.md`의 "스코프 지킴이" 섹션이 세션 시작 시 이 파일을 먼저 읽도록 지시한다
2. 1번 체크리스트는 실제 기능 목록으로 채워져 있다 — 초기 상태를 사용자가 1회 확정하면 이후 고정
3. 이후 모든 신규 요청은 2번 절차를 통과, 매 세션은 3번 갭 체크로 시작
4. 깊은 코드 대조가 필요하면 `scope-guardian` 서브에이전트(`.claude/agents/scope-guardian.md`)를 호출한다
