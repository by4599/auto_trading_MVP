# 백테스트 인프라 설계 — Phase 3의 전제 조건

> 이 프로젝트의 원칙 ②는 "백테스트 없는 규칙은 채택하지 않는다"이고,
> [성과 거버넌스](PERFORMANCE-GOVERNANCE.md)는 "백테스트 기준선 대비 괴리"로 강등을
> 판정한다. 즉 **백테스트 엔진 없이는 원칙도 거버넌스도 실행 불가능하다.**
> 이 문서는 그 인프라의 요구사항·설계·합격 기준을 정의한다.
>
> ⚠️ 현행 `VolatilityBreakoutStrategy(K=0.5)`도 검증된 적이 없다. 엔진이 완성되면
> **가장 먼저 현행 전략을 소급 검증**한다 — 지금의 K=0.5는 관례값이지 근거값이 아니다.

---

## 1. 요구사항 — 좋은 백테스트의 4조건

| 조건 | 의미 | 이 시스템에서의 구현 |
|---|---|---|
| **동일 코드** | 백테스트와 실전이 같은 전략 코드를 실행해야 함 (재구현하면 미묘하게 달라짐) | `Strategy`·`RiskRule` 인터페이스를 그대로 사용 — 기존 인터페이스 분리 설계가 이미 이를 지원 ✅ |
| **선견 편향 금지** | 그 시점에 알 수 없던 정보(미래 데이터)를 쓰지 않음 | §3.2 이벤트 타임스탬프 정합 |
| **비용 포함** | 수수료·세금·슬리피지 반영 없는 백테스트는 환상 | §2.3 체결 모델 |
| **충분한 표본** | 소표본 통계는 노이즈 | §4 합격 기준 (최소 트레이드 수) |

## 2. 엔진 설계

### 2.1 아키텍처 — 구현체 교체 방식

```
@Profile("backtest")
  BacktestMarketDataService  : DB 적재 과거 캔들을 시간 순서대로 재생
  BacktestOrderClient        : 가상 체결 (§2.3 체결 모델)
  BacktestPositionManager    : 인메모리 계좌 (현금 포함 — F-1 교정 반영)

BacktestRunner: 가상 시계를 1틱씩 전진시키며 기존
  Strategy → Signal → RiskEngine → OrderEngine 파이프라인을 그대로 통과시킴
```

- 핵심: **RiskEngine을 우회하지 않는다.** 리스크 룰까지 포함해야 "시스템 전체"의
  백테스트가 되고, 실전-백테스트 괴리 판정의 분모로 쓸 수 있다.
- `LocalTime.now()` 같은 실시간 의존은 `Clock` 주입으로 치환 (감사 F-8과 동일 작업 —
  백테스트가 이 리팩터를 강제한다).

### 2.2 데이터 적재

| 항목 | 설계 |
|---|---|
| 소스 | KIS 일봉/분봉 API (수정주가 `FID_ORG_ADJ_PRC=1` — 현행과 동일) |
| 저장 | 로컬 DB `candle_history` 테이블 — 야간 배치로 워치리스트 종목 적재 |
| 분봉 필요성 | 현행 전략은 "당일 현재가 > 돌파가" 판정이므로 **일봉만으로는 진입 시점을 모른다** (종가가 돌파가 위여도 장중 언제 돌파했는지 불명). 분봉(최소 10분봉)으로 돌파 시각과 진입가를 재현 |
| 생존 편향 | 상장폐지 종목이 빠진 데이터는 성과를 부풀린다. v1 유니버스(대형주)에서는 영향이 작으므로 **한계로 명시하고 수용**, 유니버스 확대 시(Phase 4) 재평가 |
| 기간 | 최소 3년 (상승장·하락장·횡보장을 모두 포함해야 레짐 편향 방지) |

### 2.3 가상 체결 모델 (보수적으로)

```
진입가   = 신호 발생 다음 분봉의 시가          (신호 본 그 가격에 체결됐다고 가정하지 않음)
슬리피지 = 진입가 × 0.1% × 양방향 (초기 가정값) → 실전 실측값으로 교체
비용     = 위탁수수료 + 거래세 (거버넌스 §1의 비용 상수 공유)
매도     = 타임컷/손절 동일 규칙 — 하한가 등 체결 불가 상황은 다음 봉 시가로 지연 체결
```

**왕복 총비용(슬리피지+수수료+세금 합계)은 0.3~0.5% 미만으로 잡지 않는다.**
단타 전략의 사인(死因) 1순위가 거래 비용이며, 비용 가정이 낙관적인 백테스트의
우상향 곡선은 실전에서 우하향으로 뒤집힌다. 비용을 이 수준으로 잡고도 PF가 기준을
넘는 전략만이 실전 후보 자격이 있다.

원칙: **애매하면 항상 불리하게.** 백테스트가 낙관적이면 실전에서 반드시 실망하고,
보수적이면 실전이 서프라이즈가 된다 — 후자가 생존에 유리하다.

## 3. 검증 절차 — 과최적화를 걸러내는 순서

### 3.1 Walk-Forward 검증 (파라미터 튜닝 시 필수)

```
|-- 학습 6개월 --|-- 검증 3개월 --|
        |-- 학습 6개월 --|-- 검증 3개월 --|   ← 3개월씩 밀며 반복
```

- 파라미터(K값 등)는 학습 구간에서만 정하고, 성과는 **검증 구간만** 집계한다.
- **민감도 검사**: K=0.4 / 0.5 / 0.6 성과가 급변하면 불합격 — 좋은 전략은 파라미터
  근방에서 성과가 완만하다. 뾰족한 최적점은 과최적화의 지문이다.

### 3.2 이벤트 백테스트 (Phase 3c 용)

- 이벤트(공시·뉴스) 타임스탬프와 캔들 타임스탬프의 정합이 생명:
  **"그 시점에 알 수 있었는가"** — 장 마감 후 공시를 당일 진입으로 계산하면 선견 편향.
  규칙: 이벤트 발생 시각 + 처리 지연(수집 주기 30분) 이후의 첫 봉부터 진입 가능.
- 이벤트 유형별 D+1~D+20 수익률 분포를 산출 → 평균이 아니라 **중앙값과 하위 25%**를
  기대수익으로 채택 (평균은 소수 대박이 왜곡).

### 3.3 필터 A/B 검증 — 스위치는 만들되, 켜는 건 이긴 다음

실무에서 통용되는 아래 4개 필터는 Phase 2에서 **코드 훅(설정 스위치)으로 구현하되
기본 OFF**로 두고, B-3에서 순정(필터 없음) 대비 A/B로 검증해 이기는 조합만 ON으로
전환한다. 원칙 ②는 필터에도 예외 없이 적용된다 — 파라미터 추가는 곧 과최적화 위험
추가이므로, 아무리 상식적인 필터라도 관문을 거친다:

| 필터 | 가설 | 파라미터 후보 |
|---|---|---|
| 진입 시간 창 | 장초반(09:00~09:15) 휩소성 가짜 돌파 회피 | 09:10 / 09:15 / 09:30 이후만 진입 |
| 거래량 확인 | 거래량이 동반된 돌파만 신뢰도가 높다 | 돌파 시점 누적 거래량 ≥ 전일 동시간대 × 1.5~3.0 |
| 트레일링 스톱 | 오후 흘러내림에서 수익 보존 (타임컷 보완) | +3% 도달 후 고점 대비 1~2% 하락 시 청산 |
| 지수 레짐 | 지수 급락일의 개별 돌파는 무너지기 쉽다 | KOSPI가 당일 시가 아래면 신규 진입 금지 |

**채택 판정**: 필터 적용 버전이 순정 대비 **검증 구간의 PF와 MDD 양쪽에서 우위**일
때만 ON. 한쪽만 개선되면(수익↑ MDD↑ 등) 채택 보류하고 결과만 기록한다.
조합 탐색은 단일 필터 → 상위 2개 조합까지만 (조합 폭발 = 과최적화 지름길).

### 3.4 최종 관문 — 모의투자 (이미 인프라 보유)

백테스트 통과 → 모의투자 30건 이상 → 실전 축소 자금. 이 3단 관문은
[거버넌스 §5](PERFORMANCE-GOVERNANCE.md)의 승격 절차와 동일한 사다리를 공유한다.

## 4. 합격 기준 (백테스트 → 모의 승격)

| 지표 | 기준 | 이유 |
|---|---|---|
| 트레이드 수 | ≥ 100건 | 소표본 차단 (거버넌스 §2와 일관) |
| Profit Factor | ≥ 1.3 (비용 차감 후) | 1.0 근처는 비용 오차로 뒤집힘 |
| 기대값 | > 0 (검증 구간만, Walk-Forward) | 학습 구간 성과는 채점 대상 아님 |
| MDD | 백테스트 MDD ≤ 15% | 실전은 통상 이보다 나쁘다 — 거버넌스의 1.5배 룰 분모 |
| 민감도 | 인접 파라미터에서 PF ≥ 1.15 유지 | 과최적화 지문 검사 |

합격한 전략의 **기대값·PF·MDD를 설정 파일로 버전 관리**한다 — 이것이 거버넌스가
요구하는 "백테스트 기준선"이며, 실전 괴리 판정의 분모가 된다.

## 5. 구현 로드맵

| 단계 | 작업 | 산출물 |
|---|---|---|
| B-1 | `candle_history` 스키마 + 야간 적재 배치 | 3년치 일봉/분봉 데이터 |
| B-2 | 백테스트 구현체 3종 + BacktestRunner + Clock 리팩터 | `--spring.profiles.active=backtest` 실행 가능 |
| B-3 | **현행 변동성 돌파 소급 검증** — K 민감도 + Walk-Forward + 필터 4종 A/B (§3.3) | K=0.5 채택/기각 근거, 채택 필터 목록, 기준선 파일 |
| B-4 | 이벤트 백테스트 확장 (공시/뉴스 반응 통계) — ✅ **1차 실행 (2026-07-13)**: 공시 6,688건 3년 백필, 20유형 통계 산출 (EVENT-REPORT-20260713-0022). 유일한 CANDIDATE는 INSIDER_OWNERSHIP(4,813건, D+5 중앙값 +4.7%)이나 **오염 확인 — §8 주의** | 이벤트 유형별 기대수익 테이블 (Phase 3c) |

- 위치: Phase 2와 3 사이. **B-3 결과가 나쁘면 Phase 3 착수 전에 전략 교체를 판단**할
  수 있으므로, 빠를수록 좋다 (지금은 검증 안 된 전략의 인프라를 다지고 있는 셈).

---

## 6. v1 구현 노트 (2026-07-10 — 일봉 근사)

B-1~B-3은 `com.trading.backtest` 패키지로 구현됐다 (`--spring.profiles.active=backtest`,
전용 `backtest-db` 사용 — 라이브 DB 무오염). §2.2의 분봉 요구는 **KIS가 과거 분봉을
소급 제공하지 않아** v1에서 일봉 근사로 대체했고, 대신 `MinuteCandleCollector`가
매일 15:40 당일 분봉을 축적해 미래의 정밀 재검증을 준비한다.

**일봉 근사 관례 (전부 보수 방향, §2.3 원칙 준수):**

| 항목 | v1 관례 |
|---|---|
| 진입 시각 | 10:00 고정 체크포인트 (돌파 시각 불명) |
| 진입가 | 이분탐색으로 복원한 돌파 크로싱가 + 슬리피지 0.1% — 전략 `evaluate()`의 가격 단조성만 이용, K 로직 미복제 (동일 코드 원칙) |
| 당일 진입 후 손절 | 저가 ≤ 손절가면 체결 간주 (저가가 진입 전이었을 가능성 무시 — 비관) |
| 타임컷 체결가 | 당일 종가 근사 |
| 비용 | 왕복 ≈0.41% (슬리피지 0.1%×2 + 수수료 0.015%×2 + 매도 제세 0.18%) |

**§3.3 필터 구현 상태:** 4종 모두 스위치(기본 OFF, `trading.filters.*`)로 구현.
트레일링 스톱·지수 레짐(갭다운 변형)은 일봉 A/B 가능 — B-3 리포트에 포함.
**진입 시간창·거래량 확인은 돌파 시각이 필요해 일봉으로 검증 불가** — 분봉 축적 후
별도 세션에서 A/B 전까지 켜지 않는다 (원칙 ②).

산출물: `logs/backtest/REPORT-*.md` (실행 리포트), 합격 시 `docs/BACKTEST-BASELINE.yml`
(거버넌스 기준선 — 버전 관리).

## 7. B-3 판정 결과 (2026-07-11 실행) — ❌ 불합격

기간 2023-07-11~2026-07-10, 대형주 6종목(005930·000660·373220·005380·035420·068270),
왕복 비용 0.41%, Walk-Forward 10윈도우 (REPORT-20260711-1131.md):

- **검증 구간 집계: PF 0.66, 기대값 -0.333%, MDD 50.4%** (합격선: PF≥1.3, 기대값>0, MDD≤15%)
- 10개 검증 윈도우 **전부 PF < 1.0** (최고 0.98) — 특정 구간의 불운이 아니라 구조적 열세
- K 민감도: 0.4/0.5/0.6 전기간 PF 0.57/0.52/0.53 — K 튜닝으로 구제 불가
- 필터: 트레일링 스톱 1%가 PF 0.66→0.71, MDD 50.4→44.3%로 동반 개선하나
  **PF < 1.0인 전략은 필터로 흑자 전환되지 않는다** — 채택 보류
- ~~지수 레짐(갭다운 금지)은 개선 없음 → 기각~~ ⚠️ **이 판정은 무효 (2026-07-25 확인, §14.4)**:
  `BacktestRunner`가 `loadSeries(config.symbols(), …)`로 **매매 대상 종목만** 메모리에 올리는데
  KOSPI는 그 목록에 없어 `dayBar("KOSPI")`가 항상 null → `isBearishRegime()`이 항상 `empty` →
  `IndexRegimeRule`은 켜도 **항상 통과**했다. git 이력상 B-3 최초 실행부터 줄곧 무발동이므로
  "개선 없음"은 필터가 켜진 적 없는 상태의 관측이다. paper는 이 필터를 켠 적이 없어 실계좌
  영향은 없다. 재판정하려면 지수를 리포지토리에서 직접 읽어야 한다(§14.4의 `IndexTrendRule` 방식).

**결론: 현행 "당일 변동성 돌파(대형주·일봉·왕복 0.41%)"는 비용 차감 후 음의
기대값이 3년 전 구간에서 일관되게 확인됐다. 원칙 ②에 따라 이 전략은 실전 승격
불가이며, Phase 3(이벤트 드리븐) 착수 전 전략 교체/재설계 판단이 필요하다.**

남은 검증 여지 (기각 전 확인할 것): ① 진입 시간창·거래량 필터 — 돌파 전략의
고전적 개선인데 일봉으로 검증 불가, 분봉 축적(15:40 수집) 수개월 후 A/B 가능
② 유니버스 — 대형주는 변동성 돌파와 궁합이 나쁘다는 통설이 있음 (중소형
고변동 종목 재검증 여지, 단 유동성 필터 §2.2 병행) ③ 일봉 근사의 보수 편향
(진입가 슬리피지·비관적 손절)이 실제보다 성과를 낮췄을 가능성 — 분봉 재검증으로만 해소.

## 8. B-4 1차 실행 결과와 통계 오염 주의 (2026-07-13)

공시 6,688건(3년, 6종목) 백필 → 20유형 통계. OTHER 비중을 92%→8%로 낮추는
택소노미 정밀화(정례 공시 7유형 분리) 후의 결과:

- 대부분의 이벤트성 유형(공급계약·배당·자사주·실적 등)은 이 **대형주 6종목**에서
  D+5 반응이 약하거나 음(-) — 대형주는 개별 공시에 둔감하다는 통설과 일치.
- 유일한 CANDIDATE **INSIDER_OWNERSHIP**(임원·주요주주 소유상황보고, 4,813건,
  D+5 중앙값 +4.7%, 승률 78%)은 **세 겹의 오염이 확인**되어 승격 금지:
  ① 종목 편중 — 56%가 삼성전자 ② 기간 밀집 — 86%가 2025~2026 (상승기 베타 +
  역인과: 주가가 오르면 옵션 행사·매도 보고가 늘어난다) ③ 윈도우 중첩 —
  일 단위로 뭉쳐 나오는 보고서의 D+5 수익률은 서로 겹쳐 유효 표본이 명목치보다 훨씬 작다.
- ~~v2 개선 과제~~ → **v2 실행 완료 (2026-07-13, EVENT-REPORT-20260713-0049)**:
  ⓐ KOSPI 초과수익 조정 ⓑ 5거래일 클러스터 병합 (8,340 관측 → 유효 2,757건)
  ⓒ 표본 확대 (`backtest.event-symbols` — KOSDAQ 8종목 추가, 총 14종목·공시 8,359건).

**v2 결과: CANDIDATE 0건 — 전 유형 D+5 초과수익 중앙값 음(-).**
INSIDER_OWNERSHIP은 +4.7%(오염 표본 4,813건) → **−0.79%(정제 표본 375건)로 붕괴** —
v1의 신호는 전부 시장 베타 + 클러스터 부풀림이었음이 정량 확인됐다.
원칙 ②가 의도대로 작동한 결과다: 공시 유형 단독(발생 여부만)으로는 이 표본에서
매매 엣지가 없다.

**v3 정밀화 (2026-07-13, EVENT-REPORT-20260713-0101)**: ① KOSDAQ 종목은 KOSDAQ
지수로 벤치마크 분리 (지수 759봉 적재) ② 공급계약 원문(document.xml) 파싱으로
"매출액 대비 %" 추출 (`ContractSizeParser`, 56건) → `SUPPLY_CONTRACT_BIG10`
(매출 대비 ≥10%) 버킷 신설. **결과: 대형 계약도 음(-)** — BIG10 20건 D+5 중앙값
−2.03%, 승률 25%. 크기 조건화로도 이 표본에선 구제되지 않았다 (표본 20건은 소표본 —
단정은 유보하되, "공시 확인 후 진입은 늦다"는 소문에 사서 뉴스에 파는 패턴과 일관).

남은 방향: ⓐ 표본 축적 (종목·기간 — event-symbols는 설정만으로 확장 가능)
ⓑ 분봉 필터 A/B (변동성 돌파의 마지막 확인 — 수집 중) ⓒ ~~역발상 검토~~ → §9에서 채택.

## 9. 공시 쿨다운 필터 — §8 역발상의 A/B 검증과 채택 (2026-07-13)

`DisclosureCooldownRule` (기본 OFF, `trading.filters.disclosure-cooldown.*`):
이벤트성 공시(정례 지분보고 등 제외) 후 N일간 해당 종목 신규 매수 금지.
당일 공시는 판정에서 제외 — 공시 시각 불명이라 당일 차단은 선견 편향이 된다.

B-3 검증 구간 A/B (REPORT-20260713-0735, 기준선 PF 0.66 / MDD 49.2%):

| 변형 | 트레이드 | PF | 기대값 | MDD | 판정 |
|---|---|---|---|---|---|
| 쿨다운 5일 | 823 | 0.74 | −0.263% | 32.8% | ✅ 단일 필터 중 최강 |
| 쿨다운 3일 | 951 | 0.67 | −0.319% | 41.4% | 5일에 열세 |
| **트레일링 1% + 쿨다운 5일 (조합)** | 825 | **0.81** | **−0.183%** | **27.9%** | ✅ **채택 (paper ON)** |

- §8의 "공시 확인 후 진입은 늦다" 통계가 회피 룰로서 실제 작동함을 확인 —
  B-4 이벤트 통계의 첫 실용 산출물.
- **한계 명시**: 조합을 켜도 PF < 1.0 — 전략 자체의 불합격 판정은 불변이며,
  이 채택은 파이프라인 검증용 모의투자의 출혈 축소다. 실전 승격 근거 아님.
- application-paper.yml에서 두 필터 ON (조합 구성 그대로).

## 10. 테마 파급(Spillover) 통계 — B-4 확장 (2026-07-14, 실행 대기)

사용자의 실제 투자 가설 — "반도체 호황 같은 섹터 호재가 뜨면 장비·소재 납품업체가
따라 오른다, 그 관련주를 상승 반등 신호에 탄다" — 를 통계로 옮긴 것.
§8의 자기 이벤트 통계(종목 X의 공시 → X의 수익률)와 달리, **앵커 대형주의 공시 →
같은 테마 밸류체인 종목들의 D+N 초과수익**을 잰다 (customer-supplier momentum 계열).

- 표본 구조: `backtest.event-themes`의 `<테마>-anchor` / `<테마>-chain` 키 쌍.
  파일럿은 반도체 (앵커: 삼성전자·SK하이닉스, 체인: 장비·소재·부품 19종목).
- **표본 단위는 앵커 이벤트 1건** — 체인 종목별 초과수익을 이벤트당 횡단면
  중앙값으로 접는다. §8-③과 같은 부풀림(한 이벤트가 상관된 체인 N종목 수익률로
  유효 표본을 뻥튀기)을 설계 단계에서 차단. 이벤트당 커버리지 3종목 미만은 제외.
- 진입(공시 다음 거래일 시가)·클러스터 병합(5거래일, 지수 캘린더 기준)·시장별
  벤치마크는 v2/v3 규칙 그대로. 지수 캔들이 없으면 원수익률 폴백 없이 통계 생략
  (파급은 초과수익이 정의 그 자체).
- 산출: 리포트 "테마 파급" 섹션 + `event_type_registry`에 `SPILL:<테마>:<유형>` 키.
  CANDIDATE 조건·승격 게이트(G2, 사람만)는 자기 이벤트 통계와 동일.
- 해석 지침: D+1이 작고 D+5/D+10 중앙값이 양(+)이면 "천천히 스며드는" 파급 —
  공시 확인 후 진입해도 늦지 않다는 뜻이므로 §8의 자기 이벤트와 정반대 활용이 된다.
- 실행: `--backtest.mode=events` (DART_API_KEY 대기 — §8 재실행 시 함께 산출).

## 11. 진입 트리거 실험 — "언제 타는가" 격리 비교 (2026-07-14, 실행 대기)

§10이 "무엇이 오르는가"(어떤 앵커 이벤트가 파급되는가)를 재면, 여기서는 같은
이벤트 표본에서 **진입 방식만 바꿔** 성과 차이를 잰다 (`EntryTriggerBacktester`).
사용자 가설의 후반부 — "관련주를 상승 반등 신호에 탄다" — 의 검증 장치.

- 트리거 3종: **IMMEDIATE**(공시 다음날 시가 — §10과 동일 기준선) /
  **PULLBACK_REBOUND**(눌림 후 눌림일 고가 돌파 — "상승 반등") /
  **BREAKOUT**(이벤트일 고가 돌파 — 모멘텀 확인).
- 공정 비교: 청산은 전 트리거 공통 **이벤트 D+10 종가** 고정 — 진입 타이밍만
  변수로 격리. 트리거가 D+5까지 완성 안 되면 미발동 → **발동률**로 별도 집계
  (기회 빈도 자체가 트리거의 속성 — 발동률이 너무 낮으면 통계가 좋아도 못 쓴다).
- v1은 일봉 근사: 돌파 진입가 = max(시가, 기준가) 보수 추정, 같은 봉의 눌림·반등
  동시 발생은 다음 봉으로 미룸. **분봉 축적 후 분봉 정밀 버전으로 재검증이 전제** —
  v1 결과만으로 실전 트리거를 확정하지 않는다.
- 표본 규약은 §10과 동일 (횡단면 중앙값 접기·커버리지 3·시장별 벤치마크).
  클러스터 병합만 순수 시간 기준이라 표본 수가 §10과 소폭 다를 수 있다.
- 산출: 리포트 "진입 트리거 실험" 섹션 (테마×유형×트리거별 표본·발동률·승률·
  중앙값·p25). **레지스트리에는 쓰지 않는다** — 실험이지 유형 통계가 아니다.
- 판정 지침: 어떤 트리거가 IMMEDIATE 대비 중앙값 우위 + 발동률이 실용 수준이면
  분봉 정밀 검증 → 전략화(Phase 3d) 후보. IMMEDIATE가 최강이면 "확인 후 진입해도
  늦지 않다"는 §10 해석이 강화되는 것이고, 전부 음(-)이면 §8 결론(공시 확인 후
  진입은 늦다)이 파급 경로에도 성립한다는 뜻이다.
- 실행: `--backtest.mode=events`에서 §10과 함께 산출 (DART_API_KEY 대기).

## 12. 3방식(VB/EVENT/MIX) 재검증 — 유니버스 확장·시가총액 세분화 (2026-07-20) — ❌ 둘 다 기각

지갑 칸 실험(방식1 VB·방식2 EVENT·방식3 MIX)을 실매매로 구현하기 전, §7/§8이 스스로
남겨둔 "남은 검증 여지" 중 지금 데이터로 실행 가능한 두 가지를 재검증했다 (분봉
시간창·거래량 필터는 아직 데이터 부족(수집 12일치, 수개월 필요)이라 이번엔 제외).

**VB 유니버스 확장** — "대형주는 변동성 돌파와 궁합이 나쁘다"(§7 남은 검증 여지 ②)를
확인하기 위해 기존 대형주 6종목에 중소형 고변동주 33종목(semi-chain·battery·bio·
game·robot, `LiquidityScreener` 신설로 일평균 거래대금 50억원 미만 자동 제외)을
더해 39종목으로 B-3 Walk-Forward를 재실행했다 (REPORT-20260720-1055.md):

- 검증 PF 0.60 (6종목 기준선 0.66보다 **악화**), 검증 MDD 61.4% (6종목 기준선 ~50%보다 **악화**)
- **결론: 가설 기각.** 유니버스를 넓혀도 방식1(VB)은 구제되지 않으며, 오히려 더 나빠졌다.

**EVENT 시가총액 세분화** — "대형주는 개별 공시에 둔감하다"(§8)는 정성적 통설을
정량 확인하기 위해, 기존 21개 공시 유형별 통계를 종목 그룹(LARGE=대형주,
MIDSMALL=중소형 밸류체인)으로 나눠 42개 조합으로 재집계했다 (EVENT-REPORT-20260720-1101.md):

- **42개 조합 전부 CANDIDATE 미달** (D+5 p25가 전부 왕복비용 0.41%를 못 넘음)
- MIDSMALL이 오히려 대부분의 유형에서 LARGE보다 **더 나쁜**(더 음(-)인) D+5 반응을 보임
  — "중소형주가 공시에 더 민감하게 반응해 엣지가 있다"는 가설의 **정반대 방향**.
- **결론: 가설 기각.** 시가총액을 나눠 봐도 방식2(EVENT)는 구제되지 않는다.

**종합 판정**: 방식1·방식2 둘 다 이번 재검증에서 구제되지 않았고, 방식3(MIX)은
섞을 재료 자체가 없다. **3방식 모두 실매매 승격 불가 — 지갑 칸 게이트(`trading.
bucket.event-enabled=false`/`mix-enabled=false`)는 그대로 유지, PROMOTED 승격
없음(게이트 G2 불변)**. 방식1(VB)은 계속 파이프라인 검증 목적으로만 모의투자에서
운영한다.

**부수 발견 (사고 대응)**: 이 재검증 도중 `application-backtest.yml`의
`telegram.bot-token: ""` 격리가 OS 환경변수(`TELEGRAM_BOT_TOKEN`)에 의해
무력화되어 시뮬레이션 체결이 실제 텔레그램으로 발송되는 사고가 있었다
(env var가 profile yml보다 Spring 프로퍼티 우선순위가 높음). `TradingEventListener`에
`@Profile("!backtest")`를 추가해 구조적으로 차단 — yml 값 무관하게 백테스트
프로필에서는 리스너 자체가 생성되지 않는다.

## 13. 방식2·3(이동평균 돌파·눌림목 스캘핑) 소급 검증 (2026-07-22 완료 — ❌ 둘 다 불합격)

2026-07-20 커밋 `c2ce60e`에서 방식2·3이 원래의 공시 이벤트/혼합 재료(§12에서 폐기)에서
**이동평균 정배열 돌파**(`MovingAverageBreakoutStrategy`)·**눌림목 반등 스캘핑**
(`ScalpingStrategy`)로 교체되어 **백테스트 없이** 모의투자에 가동됐다. 원칙 ②("백테스트
없는 규칙은 채택하지 않는다")를 뒤늦게라도 채우기 위해, B-3와 동일한 Walk-Forward
절차로 두 전략을 소급 검증한다.

**제약** — 두 전략은 원래 `@Profile("paper")` 전용이라 backtest 프로필에서 배제되어
있었다(B-3 결정성 보호). 이번 검증을 위해 `@Profile({"paper","backtest"})`로 확장하고,
`WalkForwardEngine`이 등록된 모든 `Strategy` 빈의 신호를 합산하는 구조이므로 VB/MA/스캘핑을
**한 번에 하나씩만 활성화**(신규 `enabled` 토글)해 전략별로 깨끗하게 측정한다. 스캘핑의
목표 익절(`takeProfitPct`)은 `DailyBarSimulator`에 대응 로직이 없어 별도로 재현했다.

**절차** — 유니버스·기간·비용은 §7(B-3)과 동일: 대형주 6종목, 2023-07~2026-07,
왕복 비용 0.41%, Walk-Forward 6/3/3. 합격 기준은 §4와 동일(트레이드≥100, PF≥1.3,
기대값>0, MDD≤15%, 인접 파라미터 민감도 유지).

구현 과정에서 워밍업 버그 하나를 발견·수정했다: `BacktestMarketDataService`의
재생 시작일 이전 여유 적재분(`WARMUP_CALENDAR_DAYS`)이 ATR(14) 기준 45일로만
잡혀 있어, MA돌파의 MA120(120거래일≈달력일 170일 안팎) 워밍업이 Walk-Forward
검증 구간(3개월) 내내 한 번도 채워지지 않아 신호가 전혀 나지 않는 결함이 있었다.
260일로 늘려 해소(`CandleBackfillService.rangeFrom()`도 같은 상수를 참조해
백필 범위가 함께 늘어남).

**결과 (2026-07-22 실행, REPORT-20260721-2343.md / REPORT-MA-20260722-0227.md /
REPORT-SCALPING-20260722-0231.md)** — 대형주 6종목, 2023-07-21~2026-07-21(또는
2023-07-22~2026-07-22, 실행 시점 기준 롤링), 왕복 비용 0.41%, Walk-Forward 6/3/3.
세 전략을 신호가 섞이지 않도록 한 번에 하나씩만 켜서 각각 독립 실행했다:

| 전략 | 검증 트레이드 | 검증 PF | 검증 기대값 | 검증 MDD | 판정 |
|---|---|---|---|---|---|
| 방식1 VB (회귀 확인) | 1,085 | 0.71 | -0.305% | 41.9% | ❌ 불합격 (§7 원판정과 구조적으로 일치 — 기간이 실행일 기준 롤링이라 숫자는 다르나 결론 동일) |
| 방식2 MA_BREAKOUT | 591 | 0.55 | -0.484% | 41.9% | ❌ 불합격 |
| 방식3 SCALPING_MOMENTUM | 1,062 | 0.33 | -0.570% | **68.4%** | ❌ 불합격 |

- **방식3(스캘핑)이 셋 중 가장 나쁘다.** 승률은 59.8%로 VB(38.2%)·MA(38.2%)보다
  훨씬 높지만, PF 0.33·MDD 68.4%로 최악 — 목표 익절(+0.8%)은 작고 ATR 손절폭은
  상대적으로 커서 "자주 조금 벌고 가끔 크게 잃는" 비대칭 손익비 구조다.
- 스캘핑 파라미터 4종(windowSize/pullbackPct/reboundPct/takeProfitPct) ±20%
  민감도는 PF 0.25~0.36 범위에 평평하게 몰려 있다 — 뾰족한 과최적화 지문은
  아니지만, 그만큼 "파라미터를 조금 바꿔서 구제될" 여지도 없다는 뜻이다
  (PERFORMANCE-GOVERNANCE §6 분류상 ④ 알파 소멸/구조적 손익비 문제에 가깝다).
- MA돌파는 K 같은 단일 튜닝 파라미터가 없어 민감도 검사는 생략했다(§4 준용 —
  대신 트레이드 수 591건으로 표본은 충분).

**결론**: 방식1·2·3 전부 소급 검증 불합격 — "완화"가 아니라 "데이터로 확인"이
이번 작업의 목적이었고, 확인 결과는 3방식 모두 실전은커녕 모의투자에서도
구조적으로 손실 전략임을 보여준다. 실매매 승격(PROMOTED)은 여전히 불가하며
게이트 G2(사람 승인)는 발동하지 않는다. 지갑 칸(EVENT/MIX)은 파이프라인 검증
목적의 모의투자로만 계속 운영할지, 사용자가 재검토할 사안이다.

## 14. Exit Lab / Risk Lab — 손익비 재설계(다일 보유) (2026-07-22) — 엣지 실재 → 리스크 축소로 첫 §4 통과 후보(§14.1)

§13에서 스캘핑이 승률 59.8%인데도 최악(PF 0.33)이라는 진단 — **손익비(평균이익÷평균
손실)가 거꾸로** — 을 출발점으로, 진입 로직은 고정한 채 출구만 재설계해 손익비를
바로잡을 수 있는지 측정했다. `--backtest.mode=exit-lab`(`BacktestOrchestrator`)로 VB·MA
진입 각각에 5개 출구 프로필(P0 기준선 / P1 타이트·당일 / P2 다일·느슨트레일 / P3
다일·조인트레일 / P4 다일·순수손절)을 Walk-Forward 스윕했다. 신규 인프라:
`ExitLabProperties`(타임컷 토글·최대보유일), `BacktestRunner` 다일 이월·최대보유일
강제청산, `BacktestMetrics`에 손익비(payoff)·평균보유일 추가.

**중대한 낙관 편향 발견·수정 (검증의 핵심)** — 1차 실행은 화려했으나(6종목 MA P2 PF 3.78·
MDD 3.7%) **일봉 근사의 낙관** 때문이었다: 이월 포지션의 트레일 청산이 밤사이 갭으로
손절 레벨을 관통해도 레벨가로 체결됐다(실제로는 더 나쁜 시가 체결). `DailyBarSimulator.
checkTrailingStop`을 `checkCarriedStop`과 동일한 보수 모델로 수정 — 어제까지 고점으로
레벨을 먼저 정하고(선견 차단), **시가 ≤ 레벨이면 시가 체결(갭 관통), 저가 ≤ 레벨이면
레벨 체결**. 수정 후 MDD가 대략 2~3배로 뛰며 1차의 화려함이 사라졌다 — 낙관이 특히
MDD를 크게 숨기고 있었다.

**결과 (정직 엔진, 왕복 0.41%, Walk-Forward 6/3/3, 진입 고정 VB K=0.5)** — 다일 프로필
(P2·P3) 위주로 요약. 리포트: `REPORT-EXITLAB-{VB,MA}-20260722-1437/1438.md`(6종목),
`-1447/1451.md`(54종목):

| 유니버스 | 진입·프로필 | 트레이드 | PF | 손익비 | 기대값 | MDD | 보유일 | 판정 |
|---|---|---|---|---|---|---|---|---|
| 6종목 | VB P0(당일, 회귀 앵커) | 1070 | 0.70 | 1.14 | -0.29% | 41% | 0 | ❌ (§7 0.71 재현 ✓) |
| 6종목 | VB P2 다일·느슨 | 179 | 1.49 | 1.65 | +0.91% | 20.3% | 12.1 | ❌ (MDD) |
| 6종목 | VB P3 다일·조임 | 296 | 1.22 | 1.47 | +0.25% | 24.9% | 6.3 | ❌ |
| 6종목 | MA P2 다일·느슨 | 109 | **1.44** | 1.52 | +1.07% | **8.6%** | 10.4 | ✅ |
| 6종목 | MA P3 다일·조임 | 185 | **2.04** | 1.97 | +1.41% | **10.8%** | 6.0 | ✅ |
| **54종목** | VB P2 다일·느슨 | 313 | 1.06 | 1.38 | -0.17% | 37% | 6.8 | ❌ (붕괴) |
| **54종목** | VB P3 다일·조임 | 441 | 1.11 | 1.19 | +0.13% | 25% | 4.2 | ❌ (붕괴) |
| **54종목** | MA P2 다일·느슨 | **396** | **1.48** | 1.45 | **+1.14%** | 21.5% | 6.9 | ❌ (MDD만) |
| **54종목** | MA P3 다일·조임 | **666** | **1.53** | 1.52 | **+0.93%** | 21.0% | 4.1 | ❌ (MDD만) |
| **54종목** | MA P4 순수손절 | 90 | 1.76 | 2.01 | +3.09% | 23.1% | 25.6 | ❌ (MDD+소표본) |

**판독 (일관되고 기계적으로 납득 가능):**

1. **손익비 재설계는 작동한다.** 당일 청산(P0·P1)은 두 진입 모두 손익비 ~1.0·PF<0.7. 다일
   보유+트레일링(P2·P3)은 손익비 1.4~2.0으로 **뒤집혔고 기대값이 양(+)**이 됐다. "손실은
   짧게 끊고 이익을 추세로 태운다"가 정량 확인됐다. 트레일 없는 P4가 약한 걸 보면 **트레일링이
   핵심**(단순 장기 보유가 아님).
2. **진입은 MA(정배열)가 VB(돌파)보다 훨씬 강건하다.** VB는 6종목→54종목에서 PF 1.9→1.1로
   **붕괴**(가짜 돌파 다발). MA는 **넓은 54종목·수백 건(396·666)에서도 PF 1.48~1.53·기대값
   양(+)을 유지** — 반도체 상승장 베타가 아니라 여러 섹터에 걸친 **실재 엣지**임을 시사한다.
3. **그러나 전부 낙폭(MDD)에서 불합격.** 넓은 유니버스 MA의 MDD는 ~21%로 위험 기준 15%를
   넘는다(6종목 MA는 8.6~10.8%였으나 그건 좁은 상승장 표본의 낙관). **막힌 곳은 기대값이
   아니라 리스크다.**

**결론**: 손익비 뒤집기(다일 트레일링 + 정배열 진입) 방향은 **실재하는 양(+) 기대값 엣지로
검증됐다**(넓은 유니버스·수백 건에서 재현). 남은 벽은 **MDD 21% > 15%** 하나다. 이는
"기준 완화" 사안이 아니라 **리스크 축소로 기준을 충족시키는** 공학 문제다 — 다음 지렛대:
ⓐ 포지션 사이징 축소(1R을 계좌 1%→0.5%로) ⓑ 동시 보유 종목/포트폴리오 히트 상한 ⓒ 지수
레짐 필터(지수가 추세 아래면 신규 진입 금지 — `FilterProperties.IndexRegime` 훅 존재).
셋 다 MDD를 직접 겨냥하며 엣지(기대값)는 보존한다. 이후 ⓓ 왕복 비용 상향 민감도 → ⓔ
분봉 정밀 재검증이 남는다.

**거버넌스·게이트**: Exit Lab 자체는 **거버넌스 기준선을 쓰지 않는다**(`writeExitLabReport`
의도적 생략 — MDD 불합격이므로). 아래 Risk Lab에서 합격 후보가 나오면 그때 기준선을 쓴다.

### 14.1 Risk Lab — 리스크 축소로 MDD 벽 넘기 (2026-07-22) — ✅ 첫 §4 검증 통과 후보

위 "막힌 곳은 MDD"라는 진단대로, 진입(MA 정배열)·출구(다일 트레일링 P3)를 고정하고
**사이징(1R 비율)·동시보유 종목수만** 스윕했다(`--backtest.mode=risk-lab`). 사이징 축소는
손익비·기대값(트레이드당)에 불변이고 MDD만 스케일다운하므로, 엣지를 보존한 채 리스크
기준을 충족시키는 정공법이다("기준 완화" 아님). 넓은 54종목·정직 엔진:

| 프로필 | 트레이드 | PF | 손익비 | 기대값 | MDD | 판정 |
|---|---|---|---|---|---|---|
| RR0 기준(1.0R·동시5) | 666 | 1.53 | 1.52 | +0.93% | 21.0% | ❌ (MDD, 회귀 앵커) |
| **RR1 하프(0.5R·동시5)** | **780** | **1.96** | 1.82 | **+1.30%** | **9.9%** | ✅ (최고 위험조정) |
| RR2 하프·집중(0.5R·동시3) | 508 | 1.92 | 1.97 | +1.16% | 6.6% | ✅ |
| RR3 쿼터(0.25R·동시5) | 731 | 1.83 | 1.74 | +1.11% | 4.6% | ✅ |
| RR4 하프·최집중(0.5R·동시2) | 320 | 1.53 | 1.67 | +0.94% | 8.6% | ✅ |

**결과**: 사이징을 줄이면 **MDD가 단조 감소(21→9.9→6.6→4.6%)하고 PF·기대값은 유지**된다 —
예측대로 리스크만 스케일다운. **4개 프로필이 §4 전부 충족**(트레이드≥100·PF≥1.3·기대값>0·
MDD≤15%). 최고 위험조정은 **RR1(0.5R·동시5)**: PF 1.96·기대값 +1.30%·MDD 9.9%·780건.
윈도우별로도 **10개 창 중 8개 양(+)**, 2024·2025·2026에 고루 분포(레짐 편중 아님, 창당
56~113건). 리포트: `REPORT-RISKLAB-MA-P3-*.md`, 기준선: `docs/BACKTEST-BASELINE-EXITLAB-MA-P3.yml`.

**검증된 후보 정의**: 진입 = MA 정배열 돌파(MA5>MA20>MA60>MA120 + MA20 상향 돌파), 출구 =
다일 트레일링(ATR×1.0 손절 + 이익 시 트레일 arm1%/trail3%, 15:15 타임컷 대신 최대 20거래일
보유), 사이징 = 0.5R(계좌 0.5%)·동시보유 5종목. 이 프로젝트 역사상 **처음으로 §4를 넘긴
백테스트 후보**다.

**⚠ 아직 실전 아님 — 남은 검증·게이트**:
1. ~~왕복 비용 민감도 미검증~~ → **완료 (2026-07-23, §14.2). 버티는 한계 왕복 0.50%, 여유 0.045%p로 얇음.**
2. **분봉 정밀 재검증 미실행** (전부 일봉 근사 — 다일 트레일 갭 체결은 보수화했으나 장중 경로는 근사).
3. **진짜 하락장 미포함** (2023~2026 표본은 전반적 우호장 — 2022형 지수 -30% 크래시를 안 겪음).
   최악 창(2025 Q1)에서도 -8%로 버텼으나 대형 약세장 스트레스는 별도 확인 필요.
4. **실전 승격은 사람 게이트**: 다일 보유는 ADR-001 Sleeve A(장기 추세추종) 경계 → **ADR-001
   재논의 + 게이트 G2(사람 승인)** 선행. 이번 작업은 검증·기록까지 — paper 기본값·리스크 룰·
   지갑 칸 불변, 실매매 전환 없음.

### 14.2 왕복 비용 상향 민감도 + 재현성 고정 (2026-07-23) — 후보 유지, 비용 여유 얇음

§14.1이 남긴 검증 1번(왕복 비용 민감도)을 측정하기 위해 `--backtest.mode=cost-lab`
(`BacktestOrchestrator`)을 신설했다. 검증 후보(MA 정배열 + P3 다일 트레일링 + RR1 사이징
0.5R·동시5)를 고정하고 **왕복 거래비용만** 0.41%→0.80%로 올리며 §4 유지 여부를 본다. 비용
상향은 **슬리피지에만** 얹는다 — 위탁수수료·매도 제세금은 법정 확정값이라 시장이 나빠져도
불변이므로, 불확실한 조각(체결 미끄러짐)만 부풀리는 것이 정직하다(`BacktestCostProperties`).

**선행 사고와 재현성 고정** — 첫 실행(2026-07-23 00:58)에서 두 결함이 드러났다. ① 비용 역산
수식에 `/2`가 빠져 슬리피지가 의도의 2배로 걸렸으나 리포트 라벨은 여전히 "0.41%"로 찍혀
결과만으로는 탐지 불가(risk-auditor가 잡음). 리포트에 **실제 적용 슬리피지·실제 왕복 컬럼**을
추가해 라벨-실제 불일치가 표에 드러나게 했다. ② 판정 기간이 `LocalDate.now()`에 묶여 실행일이
하루만 달라도 Walk-Forward 창 10개가 통째로 밀려 §14.1 앵커(780·PF1.96·MDD9.9%)가 759·1.84로
어긋났다 — 같은 RR0 프로필의 MDD가 하루 차이로 21.0%↔8.3%로 출렁였다. 이를 막으려 risk-lab·
cost-lab을 **고정 판정 창(2023-07-22~2026-07-21)·고정 54종목**으로 못 박고(`BacktestDataProperties`
의 `candidate*`), 기준선 yml 자동 기록을 **`write-baseline=true` opt-in**으로 바꿨다(대조 실행이
거버넌스 기준선을 조용히 덮어쓰던 사고 차단). 이후 `--backtest.mode=risk-lab` 한 줄이 §14.1
기준선을 **소수점까지 재현**한다(RR1 780/1.959/+1.30%/9.87%, RR0 MDD 20.96% 불합격 — 검증됨).

**정본 판정 (고정 창·54종목, C0가 §14.1 RR1을 정확 재현 → 절대 판정):**

| 프로필(왕복) | 트레이드 | PF | 기대값 | MDD | 판정 |
|---|---|---|---|---|---|
| C0 0.41%(앵커) | 780 | 1.96 | +1.298% | 9.9% | ✅ |
| C1 0.50% | 758 | 1.93 | +1.307% | 8.0% | ✅ |
| C2 0.60% | 707 | 1.54 | +0.868% | 18.6% | ❌ MDD |
| C3 0.70% | 708 | 1.65 | +0.991% | 17.5% | ❌ MDD |
| C4 0.80% | 740 | 1.69 | +1.114% | 27.1% | ❌ MDD |

**판독**: 깨지는 건 언제나 MDD다 — PF·기대값은 왕복 0.80%까지 전부 양(+)으로 살아있다.
**버티는 한계는 왕복 0.50%(편도 슬리피지 0.145%)**. 현재 모델 가정이 편도 0.100%이므로 **실전
슬리피지 여유는 0.045%p뿐 — 얇다.** MDD 절대값은 여전히 경로 민감(C4가 20.3%↔27.1%로 흔들림).

**후보 지위 판정: (a) 유효한 검증 후보로 유지 — 남은 검증 대기.** 재현성이 못 박혔고 모델 비용
(0.41%)과 한 단계 위(0.50%)에서 §4를 전부 통과하므로 기각 사유 없음. 다만 비용/MDD 여유가 얇고
병목이 잘 흔들리는 MDD라, 남은 검증 중 **약세장 스트레스가 실질 관문**이다(손실 분기 사슬을 늘려
MDD를 더 조일 방향). 통과했으나 절벽 바로 앞이다. 리포트: `logs/backtest/REPORT-COSTLAB-MA-P3-RR1-*.md`,
`REPORT-RISKLAB-MA-P3-*.md`. **paper 기본값·리스크 룰·지갑 칸 불변, 실매매 전환 없음.**

### 14.3 약세장 스트레스 — 판정 창 3년→6.5년 확장 (2026-07-25) — ❌ 후보 불합격

§14.1이 남긴 검증 3번(진짜 하락장 미포함)을 해소하려고, **코드·설정 변경 없이 판정 창만**
오버라이드해 재실행했다(`--backtest.mode=risk-lab --backtest.candidate-from=2020-01-01`,
`candidate-to`는 기본 2026-07-21). 창은 10개 → **24개**, 표본은 780 → 1,591건이 됐다.
기준선 yml은 `write-baseline=false`로 **무변경**(md5 불변 확인).

| 프로필 | 트레이드 | PF | 기대값 | MDD | 판정 (§14.1 3년 창) |
|---|---|---|---|---|---|
| RR0 기준(1.0R·동시5) | 1354 | 1.43 | +0.736% | **36.0%** | ❌ (21.0%) |
| **RR1 하프(0.5R·동시5)** | **1591** | **1.26** | **+0.561%** | **33.5%** | **❌** (PF 1.96·MDD 9.9%) |
| RR2 하프·집중(0.5R·동시3) | 1027 | 1.33 | +0.548% | 34.4% | ❌ (6.6%) |
| RR3 쿼터(0.25R·동시5) | 1634 | 1.37 | +0.819% | 23.1% | ❌ (4.6%) |
| RR4 하프·최집중(0.5R·동시2) | 651 | 0.99 | +0.211% | 37.9% | ❌ (8.6%) |

**판정: 5개 프로필 전부 §4 불합격 — 합격 프로필 없음.** RR1은 PF 1.26(<1.3)·MDD 33.5%(>15%)·
사이징 근방 PF 하한 0.99(<1.15)로 3개 항목 미달.

**파괴자는 코로나가 아니라 2022 금리 쇼크다.** RR1의 2022년 4개 검증 창이 **연속 음(-)**
(-11.57/-8.66/-6.49/-7.34%, 합성 **-30.0%**)이고, 같은 기간 KOSPI가 -24.9%였으므로
**지수보다 더 잃었다** — 롱온리 추세추종에 손절·트레일링을 얹고도 방어에 실패했다.
Q1에 신호가 14건까지 줄어(정배열 붕괴 = 현금 회피) 일부 작동했으나, Q2~Q4에 34→59→48건으로
다시 늘며 **하락 도중 반등마다 사서 잘리는 휩쏘**가 이어졌다. 낙폭 구간은 2021Q4 정점 →
2023Q3 저점, **약 21개월 침수**. 단일 최악 창은 2024Q3(-12.62%, 8/5 급락)로, 창 정렬이 바뀌자
§14.1에서 PF 0.90으로만 보였던 구간이 훨씬 크게 드러났다(MDD의 창 민감성 재확인).

**사이징 축소의 한계**: 1R 축소만 부분 효과(1.0R→0.25R에서 36.0→23.1%)이고, **동시보유 축소는
약세장에서 역효과**다(동시5→3→2에서 33.5→34.4→**37.9%**). 우호장에서 "집중이 안전하다"고
읽혔던 §14.1의 모양(9.9→6.6→8.6%)은 **레짐 착시**였다. 어느 방향으로도 15%에 닿지 않는다.

**⚠ 코로나 폭락은 아직 채점되지 않았다.** 6/3/3 워크포워드에서 `candidate-from=2020-01-01`이면
첫 창은 학습 2020-01~06 / 검증 2020-07~09라, **2020-02~03 폭락이 전부 학습에 흡수**되고
채점 최초일은 2020-07-01이다. 채점하려면 `candidate-from≈2019-07-01`이 필요하고
`WARMUP_CALENDAR_DAYS=260` 때문에 캔들 하한이 **2018-10-14 이전**이어야 하는데 현재 하한은
2019-04-01이다. 즉 **이번에 실제로 시험된 하락장은 2022 하나뿐**이며, 그것도 코로나를 빼고
낸 성적이 위 표다. 참고로 폭락 직후 대반등 창(2020-07~09, KOSPI 2,108→2,873)에서 RR1은
84건에 **-2.94%** — 표본 최대 상승 국면도 못 먹었다.

**편향은 전부 낙관 방향**이다: ① 생존편향(54종목은 오늘까지 살아남은 종목, 2020~2022
상장폐지 0건) ② 코로나 미채점 ③ 2026 급등 창(+34.19%, 154건)이 집계를 위로 끌어올림
④ 집계 MDD가 분기 단위 곡선 기준. **실제 약세장 성적은 이 표보다 나쁠 가능성이 높다.**

**후보 지위 판정: (b) 기각 — §14.1 후보는 약세장 스트레스를 통과하지 못했다.** §14.1의 남은
검증 3번은 "해소"가 아니라 "불합격으로 판명"으로 닫는다. 다만 2024~2026 10창은 여전히
8/10 양(+)·누적 +106.7%로 §14.1을 재현하므로, **엣지가 없는 게 아니라 하락 추세 레짐에서
뒤집힌다**가 정확한 진술이다. 리포트: `logs/backtest/REPORT-RISKLAB-MA-P3-20260725-0003.md`
(헤더 기간 2020-01-01~2026-07-21로 §14.1 실행과 구분), 로그:
`logs/backtest/stress-risklab-20260724-2354.log`, 판정 원문: `_workspace/17_quant_bear-stress.md`.
**기준선 yml·레지스트리 미기록, paper 기본값·리스크 룰·지갑 칸 불변, 실매매 전환 없음.**

### 14.4 지수 추세 레짐 필터 — §14.3 사인의 정면 조준 (2026-07-25) — ⚠ 조건부·잠정 통과

§14.3의 사인은 명확했다: 2022 하락 추세 중 반등마다 "정배열 회복"으로 오인해 사고 잘리는
**휩쏘**(Q1~Q4 4연속 손실, 합성 -30%). 가설 — **지수가 장기 이동평균 아래면 신규 진입 금지**하면
해소되는가. `--backtest.mode=regime-lab`(신규 `IndexTrendRule` — `RiskRule` 구현체, `RiskEngine` 무수정)로
진입·출구·사이징(MA 정배열 + P3 + RR1)을 고정하고 필터만 스윕했다. 창 2020-01-01~2026-07-21(24창).

**⚠ 기존 갭다운 필터가 무발동이었음이 이 작업에서 드러났다 — §7 정정 참조.** 신규 필터는 지수 일봉을
`CandleHistoryRepository`에서 직접 읽어(다른 통계 백테스터와 동일 패턴) 그 함정을 피했다.
실행 로그 `[IndexTrend] KOSPI 일봉 캐시 1797건`으로 실제 동작을 확인했다.

| 프로필 | 트레이드 | PF | 손익비 | 기대값 | MDD | 총수익 | 판정 |
|---|---|---|---|---|---|---|---|
| G0 필터OFF(회귀 앵커) | 1591 | 1.26 | 1.46 | +0.561% | 33.5% | +62.07% | ❌ |
| **G1 지수 MA120 이탈 시 진입금지** | **1192** | **1.51** | **1.68** | **+0.927%** | **13.3%** | **+121.59%** | **✅** |
| G2 지수 MA200 이탈 시 진입금지 | 1153 | 1.47 | 1.62 | +0.814% | 16.9% | +95.41% | ❌ (MDD) |

**회귀 앵커**: G0가 §14.3 RR1(1591 / 1.26 / 1.46 / +0.561% / 33.5%)을 **24개 창 전부 한 자리도 안
틀리게** 재현 — 실행 유효.

**2022 사슬이 끊겼다** (창별 수익률 / 트레이드):

| 창 | G0 | G1 | G2 |
|---|---|---|---|
| 2022 Q1 | -11.57% / 14 | +0.00% / **0** | 0 |
| 2022 Q2 | -8.66% / 34 | +0.00% / **0** | 0 |
| 2022 Q3 | -6.49% / 59 | +0.00% / **0** | 0 |
| 2022 Q4 | -7.34% / 48 | -4.41% / 23 | 0 |
| **합성** | **-30.01%** | **-4.41%** | ±0.00% |

2022 손실 155건 중 132건(85%)을 회피했다.

**과필터 검사 통과(핵심)** — "안 사면 안 잃는다"를 배제하기 위한 검사다. G1은 트레이드가 -25.1%
(절반 훨씬 위)에 그치면서 **PF·기대값이 함께 개선**됐고, 무엇보다 **2024~2026 상승 구간이 +106.71%
→ +113.92%로 오히려 늘었다**. 총수익은 +62.07% → **+121.59%(약 2배)**. 즉 상승 수익을 깎아 낙폭을 산
게 아니라 **손실 구간만 선택적으로 제거**됐다. G2(MA200)는 반등 초입을 놓쳐 상승 수익이 -13.2%p
깎였다 — 느린 지표의 대가.

**선견편향 없음(감사 검산 완료)**: 판정은 `closes.headMap(simDate, false)`로 **전일까지만** 사용
(MA 표본 `[simDate-N, simDate-1]`, 비교 종가 = 전일 종가). 감사자가 함수를 별도 JVM에 복사해 9종
검산 — 당일·미래 값을 극단으로 바꿔도 판정 불변, **전일만 바꾸면 뒤집힘**, 대조군(`headMap` 상한
포함)으로 되돌리면 판정이 뒤집혀 차단선이 하중을 받음을 확인.

**⚠ "조건부·잠정"인 이유 (합격 도장 아님)**:
1. **남은 MDD는 필터로 막을 수 없는 종류다.** 창 17(2024 Q3, 8/5 급락)이 G0·G1·G2에서 **완전히 동일**
   (34건 / -12.62% / 창내 MDD 16.20%) — 급락 직전 KOSPI가 장기선 **위**였기에 한 건도 못 막았다.
   G1의 MDD 13.27%는 사실상 이 한 창이며, 한도 15%까지 **여유 1.7%p뿐**. 2022형 벽은 넘었으나
   **크래시형 위험은 그대로**다.
2. **§4 5번째 기준(민감도 ±20%) 미측정.** MA120↔MA200에서 PF 1.51↔1.47로 완만한 건 고무적이나
   정식 ±20%(MA96/MA144) 스윕은 안 했다.
3. **유효 표본은 1192건보다 작다** — G1은 5개 창에서 진입 0건이고, 실질 의사결정은 "KOSPI가 MA120
   위였던 날짜"에 집중된다.
4. 미검증 이상치: 창 15에서 G1 트레이드가 G0보다 **많다**(90 > 83) — 슬롯 경합 경로 의존으로 보이나 미확인.
5. 생존편향(2020~2022 상장폐지 0건) · 코로나 미채점(§14.3) · 일봉 근사 · 비용 0.41%(§14.2 한계 0.50%) ·
   MA200 워밍업 부족으로 2020년 1월 말까지 G2는 '판단 불가'(=G0 동작) · 지수는 KOSPI 단일.

**거버넌스**: regime-lab은 기준선 yml을 쓰지 않는다(`baselineOnPass=false`, 실행 후 무변경 확인).
paper 기본값·리스크 룰·지갑 칸 불변(`IndexTrend` 기본 OFF). **실전 전환은 ADR-001(다일 보유) 재논의 +
게이트 G2(사람) 선행** — 이 절은 측정까지다.

## 관련 문서

- [INVESTMENT-METHODOLOGY.md](INVESTMENT-METHODOLOGY.md) — 원칙 ② "백테스트 없는 규칙 채택 금지"의 출처
- [PERFORMANCE-GOVERNANCE.md](PERFORMANCE-GOVERNANCE.md) — 기준선을 소비하는 강등/승격 판정
- [ADR-001](../ADR-001-multi-sleeve-risk-architecture_1.md) — Walk-Forward 재검증 주기(미결정 사항)의 상위 문서
