# BACKLOG.md — 릴리즈 이후로 미룬 것들

> SCOPE_GUARDIAN.md 2번 절차에서 "있으면 좋은 수준"으로 분류된 항목의 보관소.
> 여기 있는 것은 지금 만들지 않는다. 재검토 시점이 오기 전에 꺼내지 않는다.

## [2026-07-20] 멀티 슬리브 (Sleeve A 장기 추세추종)
- 내용: 자금을 나눠 장기 추세추종 슬리브를 병행 운영
- 왜 지금 안 하는지: ADR-001에서 명시적 구현 보류 — 릴리즈 필수 아님
- 재검토 시점: v1 릴리즈 후 ADR-001 재논의 시

## [2026-07-20] 실계좌 연동
- 내용: 모의투자 → 한투 실전 계좌 전환
- 왜 지금 안 하는지: TRADING-RULES-AUDIT의 CRITICAL 4건 해소 + 릴리즈 기준 전체 충족이 선행 조건
- 재검토 시점: 체크리스트 8항목 전부 ☑ 달성 후

## [2026-07-20] 지정가 분할 주문 (Price Jitter)
- 내용: 시장가 대신 지정가 여러 개로 쪼개서 주문
- 왜 지금 안 하는지: ADR-001 미결정 파라미터(가격 간격·주문 개수) 결정 선행. 시장가로도 파이프라인 검증 가능 — 범위 확장
- 재검토 시점: ADR-001 파라미터 확정 시

## [2026-07-20] EVENT/MIX 방식 실매매 (방식 2·3) — 대체 전략으로 갱신
- 내용: 원래는 공시 이벤트 기반·혼합 방식의 실제 매매 연동이었으나, B-4 백테스트 전 유형
  불합격(2026-07-11) + 3방식 재검증 기각(2026-07-20)으로 재료 자체가 폐기됨. 같은 날
  사용자 판단으로 방식2=이동평균 정배열 돌파(`MovingAverageBreakoutStrategy`),
  방식3=눌림목 반등 스캘핑(`ScalpingStrategy`)으로 교체해 **백테스트 없이** 모의투자
  가동 중(커밋 c2ce60e)
- 왜 지금 안 하는지(실전 승격 기준): 2026-07-22 소급 백테스트 — 기존 당일 청산 방식은
  MA돌파 PF 0.55·스캘핑 PF 0.33으로 둘 다 불합격(BACKTEST-DESIGN §13). 그러나 **출구를
  다일 트레일링으로 재설계 + 사이징 0.5R로 낮추니 MA 정배열 진입이 §4 첫 통과**
  (PF 1.96·MDD 9.9%·780건, 넓은 54종목, §14.1). **검증된 첫 후보** 등장 —
  기준선 `docs/BACKTEST-BASELINE-EXITLAB-MA-P3.yml`
- 재검토 시점: 후보의 남은 검증(왕복 비용 민감도·분봉 정밀·약세장 스트레스) 후,
  실전 전환은 ADR-001(다일 보유=Sleeve A 경계) 재논의 + 게이트 G2(사람 승인). 스캘핑은
  손익비 구조상 폐기 후보(진입 자체 재설계 필요)

## [2026-07-20] 뉴스 → 매매 자동 연동
- 내용: 뉴스 감성 분석 결과를 매매 신호로 직접 사용
- 왜 지금 안 하는지: 로드맵상 Phase 3 — 현재는 수집·추천 표시까지가 스코프
- 재검토 시점: Phase 3 착수 시

## [2026-07-20] VB 유니버스 중소형 확장
- 내용: 변동성 돌파 대상을 중소형주 33종목까지 확대
- 왜 지금 안 하는지: 3방식 재검증에서 PF 0.60·MDD 61.4%로 오히려 악화 — 기각됨
- 재검토 시점: 새로운 필터/근거가 생기면 백테스트부터 다시

## [2026-07-20] 부분청산 (Trim) 고도화
- 내용: 부분 축소 청산의 트리거·비율 세분화
- 왜 지금 안 하는지: 기본 상태머신(LiquidationService)으로 릴리즈 요건 충족 — 범위 확장
- 재검토 시점: v2 착수 시

## [2026-07-24] 약세장 방어 — 상대강도 로테이션 (하락장에서 버티는 종목 선별)
- 내용: 지수 하락 시 현금 회피에 그치지 않고, 실제로 버티는/오르는 종목(절대 모멘텀 + 지수 대비
  상대강도)으로 회전. 사용자 희망 = "코로나 때 제약처럼 지수 빠져도 반등하는 종목에 투자".
- 왜 지금 안 하는지: (1) 신규 전략 = 스코프 확장, 릴리즈 체크리스트 8항목 아님.
  (2) **"다음 위기의 수혜 섹터를 미리 찍는다"는 백테스트 불가 — 사후편향.** 코로나→제약은 사후에만
  자명하고, 승자는 위기의 원인(2020 전염병→제약, 2022 금리→에너지, 2008→금)에 묶여 터지기 전엔
  모른다. 검증 가능한 정직한 버전은 "원인 불문, 지금 지수 대비 상대강도가 높은 종목을 반응적으로
  매수"뿐(예측 아님, 관찰). (3) 채택 전 자체 워크포워드 백테스트 필수(방법론 원칙 ②).
- 구체적 후보 신호 (사용자 관찰, 2026-07-24): 지수 급락 구간에서 **저변동성(트레일링 변동폭 작은)
  종목**을 선별. 측정 가능 = 사후편향 아님(변동성은 현재 시점 계산). 정직한 검증 포인트:
  "덜 빠진다"(저변동성·저베타 방어력)는 실증적으로 튼튼하나, **"그 뒤 급등한다"는 의심** —
  역사적으로 최대 반등은 오히려 고변동성 종목에서 나온다(저변동성은 급등보다 "덜 다치고 안정 회복"
  쪽). 급등이냐 안정 회복이냐는 백테스트로 판정. 데이터 보유(54종목+KOSPI 3년) → 지수 급락 창에서
  저변동성 vs 고변동성 버킷 forward 수익 비교 실험 가능(EventStatsBacktester/EntryTriggerBacktester 계열).
- 참고: 현 후보(MA정배열)는 하락장에서 정배열이 깨져 "매수 중단→현금"으로 이미 비켜선다.
  지수 레짐 필터 훅(FilterProperties.IndexRegime) 존재(미사용). 상대강도 회전은 그 위의 별개 전략.
- 재검토 시점: 현 후보의 약세장 스트레스 결과 확인 후 / 새 전략 착수 결정 시

## [2026-07-23] 백테스트 재현성 결함 3건 (비용 민감도 검증 중 발견) — ✅ 해소 (2026-07-23)
> 해소: 고정 판정 창(2023-07-22~2026-07-21)·고정 54종목을 `BacktestDataProperties.candidate*`에
> 못 박고 risk-lab·cost-lab만 사용, 기준선 기록을 `write-baseline=true` opt-in으로 전환.
> `--backtest.mode=risk-lab` 한 줄이 §14.1 기준선을 소수점까지 재현(RR1 780/1.959/9.87%) 확인.
> 감사 CRITICAL 0·HIGH 0, 테스트 365/365. 상세: BACKTEST-DESIGN §14.2.

- 내용: ① 백테스트 기간이 `LocalDate.now()`에 묶여 있어 실행일이 하루만 달라져도
  Walk-Forward 창 10개가 통째로 밀린다 — §14.1 기준선(07-22)을 07-23에 재현할 수 없었다
  (C0 앵커: 780건·PF 1.96·MDD 9.9% → 759건·PF 1.84·MDD 7.69%). 기간 고정 지정 옵션 필요.
  ② §14.1 표본 54종목이 설정 파일에 없다(커밋된 기본값은 6종목) — 기준선을 명령 한 줄로
  재현할 방법이 저장소에 없다. ③ `risk-lab`은 합격 시 기준선 yml을 **자동 덮어쓴다**.
  대조 실행 한 번으로 거버넌스 기준선이 오늘 숫자로 갈렸다(원복 완료). 게다가 "첫 번째
  합격 프로필"을 쓰므로 RR0가 통과하는 날엔 RR1 대신 RR0가 기준선이 된다.
- 왜 지금 안 하는지: SCOPE_GUARDIAN 릴리즈 체크리스트 8항목이 아님 — 모의투자 운영·안전장치와
  무관하다. 다만 **판정의 신뢰도에 직결**되므로 다음 백테스트 판정 전에는 해소해야 한다.
- 함께 드러난 사실(별도 판단 필요): 하루 밀림만으로 같은 RR0 프로필의 MDD가 21.0% → 8.34%로
  움직였다. §14.1의 "MDD 9.9%로 §4 첫 통과"는 **±10%p로 흔들리는 추정치** 위에 서 있다 —
  후보의 지위를 사용자가 재판단할 사안.
- 재검토 시점: 다음 백테스트 판정 착수 시 (선행 과제로)

## [2026-07-23] 실전 슬리피지 실측
- 내용: 백테스트의 편도 슬리피지 0.1%는 추정치다. 모의투자 체결 로그(주문 시각 현재가 vs
  체결가)로 실측해 비용 모델에 반영.
- 왜 지금 안 하는지: 릴리즈 필수 아님. 다만 비용 민감도 판정에서 **버티는 한계가 편도 0.145%,
  여유가 0.045%p뿐**으로 나와, 실측값이 후보의 생사를 가른다.
- 재검토 시점: 모의투자 체결 표본이 쌓인 뒤 / 후보 실전 승격 논의 전

## [2026-08-14] 소스코드 코딩 지침(300줄/함수 50줄/중첩 3단계) 준수 리팩터링
- 내용: 사용자가 새로 정한 전역 코딩 지침에 맞춰 `com.trading.backtest` 패키지 5개 초과 파일
  (BacktestOrchestrator 1349→45줄 등, 30여개 파일로 분리)을 동작 불변 원칙으로 리팩터링 완료.
  Phase A(backtest, 라이브 매매와 무관)만 완료 — Phase B(`KisApiClient`·`FillProcessor`·
  `FillStateUpdater`, 라이브 체결 경로)는 보류.
- 왜 지금 안 하는지: SCOPE_GUARDIAN 릴리즈 체크리스트 8항목 중 어느 것도 완성시키지 않는
  순수 스타일 정리. 사용자 명시 요청으로 Phase A만 예외적으로 진행.
- 검증: 구현→감사(risk-auditor, CRITICAL 0·HIGH 0)→독립 재검증(paper-ops-verifier) 3단계 완료.
  515/515 테스트 통과, backtest 프로필 실기동 확인(smoke 모드, BeanCreation 에러 0),
  리팩터 전/후 계산 결과 원 단위 일치(git worktree 대조, RR1 PF 1.96·MDD 9.87% 동일).
- 재검토 시점: Phase B(라이브 체결 경로 3개 파일)는 다음 세션에 사용자 지시 시.

## [2026-08-17] ✅ 해소 — 백테스트 재현성 결함(백필 7일 슬랙)
> 2026-08-17 해소. 슬랙은 유지하되 **고정 판정 창의 끝만 예외**로 두고(`needsTailFill`),
> 채점 전 **커버리지 관문**(`CandleCoverageChecker`)을 추가했다 — `write-baseline=true` 실행은
> 미달 시 예외로 중단, 그 외는 경고+리포트 기재. 함께 회귀 앵커 자동 대조(`BaselineDriftReporter`)도
> 넣어 기준선 yml이 정본이 됐다. 검증: 543/543 통과 · risk-lab 원 단위 재현 · 관문 실차단 확인
> (`_workspace/8_impl`·`9_audit`·`10_verify_anchor-sot-and-coverage.md`).
> **남은 사각지대는 아래 별도 항목으로 등재.**
- 내용: `CandleBackfillService.missingRanges()`의 뒤쪽 공백 규칙
  `if (storedTo.isBefore(to.minusDays(7)))`이 7일 이내 공백을 "커버리지 충족"으로 보고 스킵한다.
  고정 판정 창(`candidate-to`)과 실행일 기준 `rangeTo()`의 간격이 이 슬랙 안에 들어가면
  **창 끝 며칠이 캔들 없이 채점**되고, 그 결과가 기준선으로 굳는다.
  실측: §14.1 앵커(2026-07-24 실행)는 창 끝 2거래일(07-20·07-21)이 비어 있었고,
  같은 2일을 지우면 오늘 코드로 앵커가 지표 diff 0줄로 재현된다 (`_workspace/7_quant_baseline-drift.md`).
- 왜 지금 안 하는지: SCOPE_GUARDIAN 체크리스트 8항목에 해당 없음. 다만 **재현성은 판정의 전제**라
  릴리즈 필수로 승격할지는 사용자 확인이 필요하다(에이전트가 임의 추가하지 않음).
- 영향 범위 미확인: 다른 모드(exit-lab·regime-lab·donchian-\*) 앵커도 같은 슬랙에 걸렸는지 미점검.
- 재검토 시점: 다음 백테스트 판정 착수 전 (기준선을 새로 기록하기 전에 반드시)

## [2026-08-17] 랩 리포트 제목 오기 — 돈치안·VB 리포트가 "Risk Lab"으로 찍힌다
- 내용: `DonchianLab`·`VbFilterLab`이 `writeRiskLabReport`를 재사용해, 산출 리포트 제목이
  "Risk Lab — 리스크 축소 프로필 비교"로 고정된다. 사람이 리포트를 오독할 여지가 있다.
  리팩터링이 만든 결함이 아니라 원본(`HEAD:BacktestOrchestrator.java:1118,1163,1199,1291`)에서 넘어온 것.
- 왜 지금 안 하는지: 고치면 산출물 문자열이 바뀌어 2026-08-14 리팩터링의 "동작 불변" 검증 전제가 깨진다.
  리팩터링 커밋과 분리해야 한다.
- 재검토 시점: 리팩터링 커밋 확정 후 별건 커밋으로

## [2026-08-17] ✅ 해소 (2026-08-18) — 기준선 기록 경로가 커버리지 관문을 우회하는 곳 2군데
> 해소: 호출부마다 검사를 흩는 대신 **기록 지점 한 곳**(`BacktestReportWriter.writeBaseline`)에
> 관문을 세웠다 — ① `write-baseline=true` opt-in ② 커버리지 검사 통과, 둘 다여야 쓴다.
> 어느 조건이든 미달이면 사유를 로그에 남기고 `null`을 반환한다(파일 미기록).
> 함께 `FullBacktestLab`·`SingleStrategyLab`도 채점 전 `coverageChecker.verify`를 지나게 했다.
> **부수 효과(의도됨)**: full·ma-breakout·scalping의 "합격하면 자동 기록"이 사라졌다 —
> 2026-07-23에 risk-lab·cost-lab에만 적용됐던 opt-in 결정을 나머지 모드까지 확장한 것이다.
> 검증: 테스트 547/547 통과(신규 `BaselineWriteGateTest` 4건 — 미기록 3경로 + 정상 기록 1)
> · risk-lab 5개 프로필 원 단위 재현 · ma-breakout 라이브 실행에서 기준선 파일 전부 불변 확인.
- 내용: `FullBacktestLab.java:138-141`·`SingleStrategyLab.java:112-114`가 `judgment.pass()`만 참이면
  **`write-baseline` opt-in 확인도, 커버리지 검사도 없이** `docs/BACKTEST-BASELINE(.yml/-MA/-SCALPING)`을
  덮어쓴다. 관문은 `CandidateLabRouter.prepareCandidateUniverse`에만 걸려 있는데 full·ma-breakout·
  scalping 모드는 라우터를 지나지 않는다(`BacktestModeRunner.java:76-80`).
- 지적: risk-auditor (`_workspace/9_audit_anchor-sot-and-coverage.md` MEDIUM 1)
- 왜 지금 안 하는지: **pre-existing 결함**이며 2026-08-17 변경이 만든 것이 아니다. 릴리즈 체크리스트
  8항목에 해당 없음. 다만 "오염된 기준선이 조용히 굳는" 실패 모드가 이 경로에는 아직 열려 있다.
- 재검토 시점: full/ma-breakout/scalping 모드로 기준선을 새로 찍기 전 (반드시 선행)

## [2026-08-17] 부동 창 모드의 꼬리 결측 — 슬랙 예외가 고정 창에만 걸린다 (⚠ 절반 해소 2026-08-18)
> 2026-08-18: **기준선으로 굳는 경로는 막혔다** — 부동 창 모드도 채점 전 커버리지 검사를 지나고,
> 미달 상태에서는 기록 관문이 기준선을 거부한다. **다만 백필이 그 꼬리를 채우지는 않는다.**
> 실측(2026-08-18 `ma-breakout` 실행): 창 끝 2026-08-17인데 마지막 캔들 2026-08-13,
> 결측 거래일 1일(08-14) — 경고로 드러났다(08-15 광복절·08-17 대체공휴일은 정확히 제외).
> 즉 **조용하던 문제가 보이게** 됐을 뿐, 부동 창 채점 자체는 여전히 꼬리가 밀린 데이터로 돈다.
- 내용: 부동 창 모드(판정 창 끝 = `rangeTo()` = 실행일−1)는 `needsTailFill`이 고정 창
  (`mustCoverThrough`)만 보므로, 고정 창이 이미 채워져 있으면 슬랙 안에서 **`storedTo`가 최대
  7달력일(≈5거래일) 뒤처진 채 채점**될 수 있다. §14.1을 망친 것과 같은 결함 구조다.
- 지적: risk-auditor (MEDIUM 2). 구현자 보고 §6.2의 "부동 창 모드는 구조적으로 어긋남이 없다"는
  **반증됨** — 두 문서를 모두 남겨 뒀다(`10_verify` §8).
- 왜 지금 안 하는지: 위 항목과 같은 경로에서만 기준선으로 굳는다. 사용자 판단 대기.
- 재검토 시점: 위 항목과 함께

## [2026-08-17] 커버리지 관문이 종목별 부분 결측을 못 본다
- 내용: `CandleCoverageChecker`가 프런티어를 전 종목 **max**로 잡아, 54종목 중 1종목만 최신이면
  `sufficient=true`가 된다. 그런데 `CandleBackfillService.backfillExtra`는 종목별 예외를 잡아
  warn만 남기고 계속하므로, **30종목 백필이 실패해도 strict 실행이 통과**해 오염된 기준선이 굳을 수 있다.
- 지적: risk-auditor (MEDIUM 3). §14.1 사고는 전 종목 동일 결측이라 이 사각지대에 걸리지 않았다.
- 왜 지금 안 하는지: 이번 관문이 잡는 주 실패 모드(전 종목 꼬리 결측)는 막았고, 부분 결측은
  아직 실제로 겪지 않았다. 고치려면 종목별 커버리지 판정이 필요해 범위가 커진다.
- 재검토 시점: 백필이 종목 단위로 실패한 로그가 실제로 관측되면 즉시

## [2026-08-17] 다른 모드의 과거 기준선·앵커가 같은 슬랙에 걸렸는지 미점검
- 내용: exit-lab·regime-lab·donchian-*·crash-vol의 **이미 기록된** 기준선·앵커도 §14.1처럼
  판정 창 끝자락이 빈 상태에서 찍혔을 수 있다. 앞으로의 실행에는 두 안전장치가 걸리지만
  과거 기록은 그대로다.
- 왜 지금 안 하는지: 작업 A·B 범위 밖으로 명시 제외했다. 점검하려면 각 모드의 기록 시점
  DB 커버리지를 되짚어야 해(§7_quant가 쓴 방법) 모드당 실행 비용이 든다.
- 재검토 시점: 해당 모드의 결과를 인용해 판단하기 전 / 그 모드 기준선을 갱신할 때

## [2026-08-20] 운영 상태 거울 — Supabase 미러 (사용자 요청으로 구현, 릴리즈 항목 아님)
- 내용: 매매·기록의 원본은 로컬 H2 그대로 두고, 운영 스냅샷(운전 상태·총자산·오늘 등락률·
  연속손실·연속 무중단 가동일·보유 종목의 수량/평단/현재가/손절선/지갑칸)만 5분마다
  Supabase 테이블의 한 행에 덮어써 밖에서(휴대폰) 조회할 수 있게 한다.
  `com.trading.mirror`, paper 프로필 전용, 기본 OFF(url·key 미설정이면 스킵).
- 왜 릴리즈 항목이 아닌지: SCOPE_GUARDIAN 체크리스트 8항목 중 어느 것도 완성시키지 않는다.
  2026-08-20 사용자가 방식을 직접 선택(거울 방식)해 예외적으로 진행 — **매매 경로 무변경**이 조건.
- 매매를 건드리지 않기 위한 설계 4가지:
  ① 전송 실패는 삼킨다(로그만) — 미러가 매매를 멈추게 하지 않는다
  ② 거래일 09:00~15:30에만 전송 — 장외에는 미러 때문에만 잔고 API가 호출되고, 그 실패가
     `KisApiClient`의 연속 실패 카운터에 쌓여 **아침을 SAFE_MODE로 시작시키는** 경로가 생긴다.
     장중에는 RiskMonitor가 이미 받아둔 3초 캐시에 얹혀 KIS 호출을 새로 만들지 않는다
  ③ `@Profile("paper")` — backtest에 OS 환경변수가 남아 있어도 빈 자체가 생성되지 않는다
     (텔레그램이 환경변수로 백테스트에 샜던 사고와 같은 차단 방식, BACKTEST-DESIGN §12)
  ④ 서버 전용 secret key는 환경변수로만 주입, 테이블은 RLS ON·정책 없음 → 공개키로는 접근 불가
- 남은 것(하지 않음): 히스토리 적재(지금은 "현재 상태" 한 줄만), 원격 대시보드 화면,
  실전(real) 프로필 확장
- 재검토 시점: 히스토리·원격 화면이 필요해지면 / VPS 이전 논의와 함께

## [2026-08-21] DB 파일 백업 — 컴퓨터 밖으로 (구현 완료, 릴리즈 항목 아님)
- 내용: `backup-to-supabase.ps1` — H2 `BACKUP TO`로 `trading-db`·`backtest-db`의 온라인
  스냅샷을 zip으로 뜨고, Supabase Storage(`db-backup` 버킷)에 올린 뒤 로컬 5개·원격 7개만
  남긴다. `-MirrorDir`로 구글드라이브 등 동기화 폴더에도 복사 가능. 절차는 OPERATIONS §9.
- 왜 필요했나: 기존 `backup.sh`는 **같은 디스크**의 `backup/`에 `trading-db`만 복사했고,
  그마저 한 번도 실행된 적이 없었다(폴더 비어 있음). 분봉은 KIS가 과거를 소급 제공하지 않아
  파일이 날아가면 **영구 손실**이고, 거래 기록도 재생성 불가다.
- **앱 무변경**: 매매 앱 코드를 한 줄도 건드리지 않는다(스크립트 + 윈도우 작업 스케줄러).
  앱이 켜진 채로 떠도 정합성이 보장된다 — 단순 파일 복사와 다른 점.
- 왜 릴리즈 항목이 아닌지: 체크리스트 8항목이 아니다. 다만 사고가 나면 되돌릴 수 없는
  종류라 사용자 선택(2026-08-21)으로 먼저 진행했다.
- 검증: 실행 3회 — trading-db 18MB→4.5MB(0.6초), backtest-db 13MB→4.0MB(0.4초),
  보관 개수 정리 동작 확인(`-KeepLocal 1`로 오래된 4개 삭제). **업로드 경로는 미검증**
  (secret key 미설정) — 버킷 생성 + 환경변수 후 첫 실행에서 확인 필요.
- 남은 것: `backup.sh`(구형, trading-db만 같은 디스크에 복사)는 그대로 두었다 — 삭제 여부는
  사용자 판단. 거래 기록 표 누적(연산·조회용)은 아직 안 함.
